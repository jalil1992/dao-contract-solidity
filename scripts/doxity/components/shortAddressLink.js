    const urlPrefix = interaction.network === '2' ? 'testnet.' : ''
  render() {
    const urlPrefix = interaction.network === '2' ? 'testnet.' : ''
  render() {
    const urlPrefix = interaction.network === '2' ? 'testnet.' : ''
    const urlPrefix = interaction.network === '2' ? 'testnet.' : ''
    const urlPrefix = interaction.network === '2' ? 'testnet.' : ''
    const url = `https://${urlPrefix}etherscan.io/address/${address}`
  render() {
  render() {
  render() {
    const url = `https://${urlPrefix}etherscan.io/address/${address}`
    const urlPrefix = interaction.network === '2' ? 'testnet.' : ''
    const url = `https://${urlPrefix}etherscan.io/address/${address}`
  render() {
  render() {
  render() {

